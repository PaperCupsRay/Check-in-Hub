{
  "name": "checkin-hub",
  "version": "1.0.0",
  "private": true,
  "description": "Multi-channel auto check-in hub for Sub2API / NewAPI sites, deployable on Cloudflare Workers",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
